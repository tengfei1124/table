<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <link rel="stylesheet" href="/css/list.css">
    <script src="/js/list.js"></script>
</head>
<body>
    <table>
        <tr>
            <th>姓名</th>
            <th>性别</th>
            <th>年龄</th>
            <th>操作</th>
        </tr>
        <%if(result.length==0){%>
            <td conspan="4">没有任何信息</td>
        <%}else{%>
        <%for(var i=0;i<result.length;i++){%>
        <tr>
            <td><%=result[i].name%></td>
            <td>
            <%if(result[i].sex==1){%>
                男
            <%}else if(result[i].sex==2){%>
                女
            <%}else{%>
                保密
            <%}%>
            </td>
            <td><%=result[i].age%></td>
            <td>
                <a href="/del/<%=result[i].id%>">删除</a>
                <a href="/edit/<%=result[i].id%>" class="edit">编辑</a>
            </td>
        </tr>
        <%}%>
        <%}%>
    </table>
    <a href="/add/" class="add">+</a>
    <!--<img src="/img/list.png" alt="">-->
</body>
</html>